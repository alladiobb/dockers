#listar o dockercompose
docker ps

#rodando a imagem
docker-compose up

#rodando a imagem e liberando o terminal
docker-compose up -d

#descer os serviços
docker-compose down

#rebuildando a imagem
docker-compose up -d --build