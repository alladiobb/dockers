#lista os arquivos
docker ps
#como se fizesse hellow-world:latest (Pega a ultima versão)
docker run hellow-world

#se ele executar e não aparecer quando você digitar comando:
docker ps
#tente
docker ps -a
#ele irá mostrar o histórico
